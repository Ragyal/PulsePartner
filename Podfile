# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PulsePartner' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PulsePartner
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Storage'
  pod 'Firebase/Analytics'

  pod 'FirebaseUI/Storage'

  pod 'CropViewController'

  target 'PulsePartnerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PulsePartnerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'PulsePartner WatchKit App' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for PulsePartner WatchKit App

end

target 'PulsePartner WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PulsePartner WatchKit Extension

end
